import React from 'react'
import styles from "../component-resume-header/Header.module.css"
type Props = {}

function Header({}: Props) {
  return (
    <div className={styles.header}>
      Header
    </div>
  )
}

export default Header